import React from "react";

import { Layout } from "antd";

const { Header: AntdHeader } = Layout;

const Header = () => (
  <AntdHeader style={{ background: "#fff" }}>Header</AntdHeader>
);

export default Header;
